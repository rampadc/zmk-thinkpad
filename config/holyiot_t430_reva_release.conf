# Normal operation: retain RTT console support for emergency printk output but
# remove deferred application logging and its runtime/power overhead.
CONFIG_LOG=n
CONFIG_ZMK_LOGGING_MINIMAL=y
CONFIG_UART_CONSOLE=n
